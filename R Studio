> beer_reviews <- read.csv("~/Downloads/beer_reviews/beer_reviews.csv")
>   View(beer_reviews)
> summary(beer_reviews)
> install.packages("ggplot2")
> library("ggplot2")
> ggplot(beer_reviews) + aes(x = beer_abv) + geom_bar()
> qplot(review_overall, data = beer_reviews)
> qplot(review_overall, data = beer_reviews, geom = "density")
> ggplot(beer_reviews,aes(x=beer_style))+geom_bar()
